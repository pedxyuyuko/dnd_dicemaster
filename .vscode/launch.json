{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run TelegramBot",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "showLog": true
        }
    ]
}